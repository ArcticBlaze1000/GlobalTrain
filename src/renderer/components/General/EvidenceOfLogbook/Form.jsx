import React from 'react';

const Form = () => (
    <div className="p-4">
        <h2 className="text-xl font-bold">Evidence of Logbook</h2>
        <p className="mt-2 text-gray-600">This form will check a folder for a photo, not a questionnaire.</p>
    </div>
);

export default Form; 